---
title: 为fuwari添加置顶功能
published: 2025-02-25
description: 为fuwari添加置顶功能
tags: [typescript, 博客]
category: 技术博客
---

为文章添加权重,权重越小排行越前
以下代码会造成归档页面排序改变,想不变只需把原来的3个函数复制一遍改名替换到`src\pages\archive.astro`即可

```ts ins={12-13}
// src/content/config.ts
const postsCollection = defineCollection({
  schema: z.object({
    /* 省略 */
    lang: z.string().optional().default(""),
    // 新增：weight字段（数字类型，可选，默认999）
    weight: z.number().optional().default(999),
    /* For internal use */
    prevTitle: z.string().default(""),
    prevSlug: z.string().default(""),
    nextTitle: z.string().default(""),
    nextSlug: z.string().default(""),
  }),
});
```

```diff lang="astro"
// src\components\PostCard.astro
interface Props {
	class?: string;
	entry: CollectionEntry<"posts">;
	title: string;
	url: string;
	published: Date;
	updated?: Date;
	tags: string[];
	category: string | null;
	image: string;
	description: string;
	draft: boolean;
	style: string;
+  weight: number;
}
const {
	entry,
	title,
	url,
	published,
	updated,
	tags,
	category,
	image,
	description,
	style,
+  weight,
} = Astro.props;
<!-- 省略 -->
<div class:list={["card-base flex flex-col-reverse md:flex-col w-full rounded-[var(--radius-large)] overflow-hidden relative", className]} style={style}>
    <div class:list={["pl-6 md:pl-9 pr-6 md:pr-2 pt-6 md:pt-7 pb-6 relative", {"w-full md:w-[calc(100%_-_52px_-_12px)]": !hasCover, "w-full md:w-[calc(100%_-_var(--coverWidth)_-_12px)]": hasCover}]}>
+        <!-- 新增：置顶标签 -->
+        {weight < 999 && (
+          <span class="bg-[var(--primary)] text-white text-xs px-2 py-1 rounded-md mb-2">置顶</span>
+        )}
<!-- 省略 -->
```

```diff lang="astro"
// src\components\PostPage.astro
<div class="transition flex flex-col rounded-[var(--radius-large)] bg-[var(--card-bg)] py-1 md:py-0 md:bg-transparent md:gap-4 mb-4">
    {page.data.map((entry: CollectionEntry<"posts">) => (
        <PostCard
                entry={entry}
                <!-- 省略 -->
                style={`animation-delay: calc(var(--content-delay) + ${delay++ * interval}ms);`}
+                weight={entry.data.weight}
        ></PostCard>
    ))}
</div>
```

```ts ins={7-13}
// src\utils\content-utils.ts
async function getRawSortedPosts() {
  const allBlogPosts = await getCollection("posts", ({ data }) => {
    return import.meta.env.PROD ? data.draft !== true : true;
  });

  const sorted = allBlogPosts.sort((a, b) => {
    // 先按权重升序，再按发布时间降序
    const weightA = a.data.weight;
    const weightB = b.data.weight;
    if (weightA !== weightB) {
      return weightA - weightB;
    }

    const dateA = new Date(a.data.published);
    const dateB = new Date(b.data.published);
    return dateA > dateB ? -1 : 1;
  });
  return sorted;
}
```

归档页面添加样式

```diff lang="svelte"
// src\components\ArchivePanel.svelte
<!-- 省略 -->
    <!-- date -->
    <div
      class="w-[15%] md:w-[10%] transition text-sm text-right text-50"
    >
+      {#if post.data.weight < 999}
+        <span
+          class="bg-[var(--primary)] text-white text-xs px-2 py-1 rounded-md"
+          >置顶</span
+        >
+      {/if}
      {formatDate(post.data.published)}
    </div>
		省略
```
